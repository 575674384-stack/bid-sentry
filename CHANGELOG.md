# Changelog

## v1.2.1

- 修复真实招标文件全被拒收：DOCX 入口预检对 `word/document.xml` 误设 1 MiB 上限，真实文档普遍超过；预检现在只验证主文档部件存在性，不再读取其内容。
- 修复含目录条目（`word/`、`_rels/` 等）的 DOCX 在清洗/生成时报「压缩包包含不安全结构」类错误： yazl 不允许目录条目携带 compress 选项。
- 标题识别适配真实中文招标文件：从 styles.xml 的样式名/大纲级别解析标题（兼容 Word/WPS 本地化 styleId），降级误标大纲级别的正文段落，并在完全没有标题样式时回退到「第X章/一、/附件格式」等保守启发式。
- 模板候选识别适配真实招标文件：关键词覆盖「招标投标各种文件格式」「资格（预/送/后）审」等变体；章节边界过小的候选会提供「延伸至文档末尾」的备选范围（明确标注请人工核对）。
- 字段匹配适配真实模板写法：标签容忍内部空格（法定 代表人）、扩充同义词（企业名称/单位名称/Bidder Name 等）并统一规划器与 OOXML 写入器的标签词表。
- 资格标生成不再因模板留空项（签字/盖章/日期等待人工填写空白）而整体阻止；只有投标单位名称找不到明确字段时才阻止，其余空白在计划中明确列出。

## v1.2.0

- 界面重构：迁移到 Ant Design 组件体系并做品牌化主题（深色侧边栏 + 松绿/琥珀点缀），统一表单、表格、步骤条与按钮；删除冗余说明文案。
- 更新面板重做：检查/下载有明确的进度条与状态标签，下载百分比实时推送；更新说明以纯文本展示，不再露出 Markdown 标记；去掉重复状态文案。
- 修复设置页在特定内容下出现横向滚动条与卡片溢出的问题。

## v1.1.1

- 修复 AI 功能在真实 OpenAI 兼容接口上完全不可用：聊天响应信封契约误用严格模式，把真实服务商必然携带的 metadata 字段（id/model/usage/finish_reason 等）当作非法响应拒绝，导致勾选 AI 后对照审查必然失败、资格标分析静默退回本地规则。
- 修复设置页保存校验失败时只显示「发生内部错误」：现在给出具体原因（如 Base URL 必须使用 HTTPS）。
- 修复对照审查/资格标页面在设置保存后仍显示旧接口与公司资料的问题。
- 修复开发/未打包环境下更新面板把 Electron 版本误显示为应用版本的问题。

## v1.1.0

- 隐私清洗：创建时间/修改时间/打印时间等文档历史时间改为保留原值，不再随机化；制作人、组织等身份类字段仍会随机重置。
- 输出位置设置化：清洗、审查和资格标生成结果始终保存在原文件所在目录，不再每次选择输出目录；设置页可选用「后缀另存」（默认 `_已清洗`，同名自动追加序号）或「验证后原子覆盖原文件」（失败自动回滚原字节）。
- 新增设置页：输出方式/后缀、公司资料预填（用于资格标表单）、托盘/启动更新检查开关。
- 资格标预制作重做：AI（或本地规则）只总结资格要求并列出模板待填字段，用户填写公司信息后按确认计划填充；有证据的固定值才填入，未知项不猜测。
- 技术：设置持久化升级到 v3 格式并兼容迁移旧文件；新增资格标分析/计划 IPC 契约，移除输出目录选择 IPC。

## v1.0.0 — 计划发布

这是首个完整产品版本，开发期间不发布中间版本。

- 增强 DOCX/PDF 元数据清洗：字段名、原值、随机新值预览，预览计划与执行一致，输入只读并验证后发布。
- 增加脱敏诊断阶段、错误码、诊断编号和本机诊断目录。
- 增加可选系统托盘行为，关闭窗口后可隐藏而不是退出。
- 增加固定官方 GitHub Releases 的确认式更新检查、下载和安装提示。
- 增加招标文件/投标文件对照审查：确定性规则优先，可选 OpenAI 兼容 AI 辅助，所有结果带证据锚点。
- 增加资格标预制作：复用用户确认的招标模板，表单值与有证据固定值可追溯，图片/证照/签章使用占位符。
- 增加 Windows/Linux x64 CI、生产包审计、开发态和打包功能 E2E。

已知限制：OCR 暂不支持；扫描 PDF 的审查和预制作需要可靠文本层；PDF→DOCX 是结构化重建而非像素级复刻；发布包当前未做代码签名；AI 结果和生成文档都必须人工复核。
