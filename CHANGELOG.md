# Changelog

## v1.1.0 — 未发布

- 隐私清洗：创建时间/修改时间/打印时间等文档历史时间改为保留原值，不再随机化；制作人、组织等身份类字段仍会随机重置。
- 输出位置设置化：清洗、审查和资格标生成结果始终保存在原文件所在目录，不再每次选择输出目录；设置页可选用「后缀另存」（默认 `_已清洗`，同名自动追加序号）或「验证后原子覆盖原文件」（失败自动回滚原字节）。
- 新增设置页：输出方式/后缀、公司资料预填（用于资格标表单）、托盘/启动更新检查开关。
- 资格标预制作重做：AI（或本地规则）只总结资格要求并列出模板待填字段，用户填写公司信息后按确认计划填充；有证据的固定值才填入，未知项不猜测。
- 技术：设置持久化升级到 v3 格式并兼容迁移旧文件；新增资格标分析/计划 IPC 契约，移除输出目录选择 IPC。

## v1.0.0 — 计划发布

这是首个完整产品版本，开发期间不发布中间版本。

- 增强 DOCX/PDF 元数据清洗：字段名、原值、随机新值预览，预览计划与执行一致，输入只读并验证后发布。
- 增加脱敏诊断阶段、错误码、诊断编号和本机诊断目录。
- 增加可选系统托盘行为，关闭窗口后可隐藏而不是退出。
- 增加固定官方 GitHub Releases 的确认式更新检查、下载和安装提示。
- 增加招标文件/投标文件对照审查：确定性规则优先，可选 OpenAI 兼容 AI 辅助，所有结果带证据锚点。
- 增加资格标预制作：复用用户确认的招标模板，表单值与有证据固定值可追溯，图片/证照/签章使用占位符。
- 增加 Windows/Linux x64 CI、生产包审计、开发态和打包功能 E2E。

已知限制：OCR 暂不支持；扫描 PDF 的审查和预制作需要可靠文本层；PDF→DOCX 是结构化重建而非像素级复刻；发布包当前未做代码签名；AI 结果和生成文档都必须人工复核。
